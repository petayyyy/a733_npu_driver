TASK B5-qwen-size-sweep: Benchmark which Qwen2.5 model sizes are practical on the Orange Pi Zero 3W
CPU (5.7 GB RAM), each across the SAME core configurations used in B4b, to find the best size and
thread layout for the user's hardware (esp. with ROS2 needing CPU/RAM headroom). This extends B4b
(which covered only Qwen2.5-0.5B) to larger sizes. CPU only; the NPU does not run Qwen (vendor-
gated, proven).

MODELS (download GGUF; use Q4_K_M to fit RAM, plus Q8_0 only where it fits, for a quality knob):
- Qwen2.5-0.5B-Instruct (baseline, already measured in B4b — re-run for consistency)
- Qwen2.5-1.5B-Instruct
- Qwen2.5-3B-Instruct
- Qwen2.5-7B-Instruct (attempt; expected to be too big / too slow — record whether it even loads)
For each: try Q4_K_M first (smallest). Add Q8_0 for 0.5B and 1.5B if RAM allows.

CORE CONFIGURATIONS (same as B4b — use taskset for affinity):
- 1x A55 (-c 0), 2x A55 (-c 0,1), 3x A55 (-c 0,1,2), 4x A55 (-c 0,1,2,3)
- 1x A76 (-c 6), 2x A76 (-c 6,7)
- 4 mixed (-c 0,1,6,7), 6 mixed (-c 0,1,2,3,6,7), 8 all (-c 0-7)
(Confirm the actual A55/A76 core index mapping on this board first via /sys/devices or lscpu, since
B4b established A76 = cores 6,7; verify and adjust if needed.)

DO (on the Orange Pi, 192.168.31.225, ROS2 stopped so the CPU is free for clean measurement):
1. For EACH (model, quant): first confirm it LOADS without OOM (check free -h before/after; note
   peak RSS). If it OOMs or swaps (SSH becomes unresponsive), record "does not fit" and skip its
   sweep. This answers "which Qwen sizes even fit 5.7 GB".
2. For each model that fits, run the full core sweep above. Per run, on a fixed prompt generating
   ~128 tokens at ctx=2k, capture:
   - decode tok/s and prefill tok/s (from llama output),
   - average AND peak CPU% via pidstat/top for the llama process (per-core% can exceed 100%, plus
     normalized %-of-8-cores),
   - which cores were actually used (taskset -cp / mpstat -P ALL),
   - peak RSS,
   - temperature at end of the longest run (cat /sys/class/thermal/thermal_zone*/temp).
3. Identify, per model, the optimal core config (best decode tok/s) — expect 2x A76 to win for
   decode (memory-bound; B4b showed extra cores hurt), but verify per size.
4. Add a "ROS2 headroom" assessment per (model, optimal config): RAM left after the model, and how
   many cores the model occupies (so the user knows what's free for ROS2 + picoclaw running
   concurrently).

DELIVERABLE: reports/b5-qwen-size-sweep.md with:
- A "fits / doesn't fit" table (model x quant -> loads y/n, peak RSS, free RAM after).
- Per fitting model, a core-sweep table (rows = the 9 core configs; columns = decode tok/s, prefill
  tok/s, avg CPU%, peak CPU%, cores used, peak RSS, temp), matching B4b's format.
- A summary recommendation: the best size/quant/core-config for (a) max speed, (b) best
  intelligence that's still usable, (c) running concurrently with ROS2 (RAM + cores free). Tie back
  to B4b's 0.5B numbers. Mark every number verified.

SUCCESS GATE: measured fit + per-core-config tok/s/CPU%/RAM for every Qwen2.5 size that loads on the
board (0.5B/1.5B/3B, 7B attempted), with a clear size+config recommendation including ROS2 headroom.
Committed.

DO NOT: run the sweep with ROS2 active (skews numbers — note that concurrent use will be slower);
use the NPU for Qwen (vendor-gated); offer sizes that OOM as recommendations.

START FROM: the B4b llama.cpp build + methodology (pidstat/mpstat/taskset, the 9 core configs, the
A76=6,7 mapping); Qwen2.5-0.5B/1.5B/3B/7B-Instruct GGUF from HF; the Orange Pi at 192.168.31.225.