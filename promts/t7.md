TASK T7-w8a16: The int8-quality research concluded the viable int8 route is
W8A16: per-channel int8 weights plus int16 activations, with offline
SmoothQuant-style smoothing. Do not base this task on the broken ACUITY hybrid
path or QDQ-ONNX import; both are confirmed dead ends for this repo.

Goal: produce a coherent W8A16 build that reduces weight memory versus int16
and ideally improves speed. Treat this as an optimization after the Orange Pi
Qwen2.5 int16 path is working, not as the production blocker.

Hypothesis to test: the int8 garbage comes from quantizing transformer-body
activations, not from the graph edges. Keep all activations, attention, softmax,
RMSNorm, RoPE, embedding, and lm_head at int16. Quantize only transformer
linear/MLP weights to int8 PCQ.

Use ACUITY host simulator plus per-layer cosine as the fast loop. Do not touch
the board until the host cosine gate passes.

Required work:
1. In PyTorch, apply SmoothQuant-style activation smoothing before exporting
   the fixed-window ONNX. Migrate per-channel activation outliers into preceding
   weights via per-channel scales. This is weight re-parameterization and does
   not require new ACUITY runtime features.
2. Configure ACUITY for W8A16: int8 PCQ weights and int16 activations
   everywhere. Use per-layer precision control.
3. Compare ACUITY host int8/int16 simulation with the FP oracle per layer.
   GO/NO-GO: end-to-end host cosine should stay above 0.99. If a layer
   collapses below 0.98, likely first/last transformer blocks, force that layer
   back to int16 and rerun on host.
4. Only after host cosine passes, export to NBG and run on the Orange Pi.
   Verify coherent text against the FP oracle, and record tok/s, peak RSS, NBG
   size, create time, first-token latency, and NPU profile time.

Optional parallel spike:
Try the quantized-TFLite import route through acuitylite TFLiteLoader, where
ACUITY says there is "no need to quantize". The purpose is scale control:
provide int8 scales yourself and bypass ACUITY calibration. Keep this spike
limited to int8/per-tensor activations.

Do not retry these confirmed dead ends:
- QDQ-ONNX import preserving Q/DQ scales;
- per-token or dynamic activation quantization;
- LLM.int8() runtime outlier split;
- etnaviv for transformer ops;
- ORT VIPLite EP;
- waiting for the full ACUITY hybrid YAML dump on the large-vocab graph.

Optional hybrid-bug workaround:
If you want to revive the hybrid path, use the completed intermediate
`<name>_pcq.quantize.json` and reconstruct the final YAML `<name>_pcq.quantize`
from it, or reduce the `dtype_converter` count by keeping larger connected
subgraphs at one precision.

Success gate:
A W8A16, or per-layer mixed, Qwen2.5-0.5B build is coherent on the Orange Pi NPU
with smaller NBG/RSS than int16, and host per-layer cosine is logged. If W8A16
still degrades after widening sensitive layers, that is a useful logged result:
int16 remains the production path.
