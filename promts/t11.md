TASK T11-qwen-chunked-bf16: Two independent research passes converged on the Qwen2.5-0.5B NBG
blocker. Key finding (verified primary source, ST Edge AI forum wrapping the same Vivante NBG
compiler): vnn_VerifyGraph status -3 / error 64768 is a per-tensor DIMENSION LIMIT of 65536
("dimension must be in the range [0, 65536["). Qwen's vocab is 151936 > 65536, so the lm_head /
logits tensor almost certainly trips it. Also verified (TIM-VX source): BF16<->INT16(DFP)
boundaries are illegal inside SLICE/MATMUL; the legal bridge is a DEDICATED DataConvert/Cast node
(never a SLICE/MATMUL straddling two dtypes). Goal: a coherent, exportable Qwen2.5-0.5B W=32 NBG.

GATE 1 (cheap, decisive — test the 65536 theory FIRST, before anything heavy):
1. Modify make_real_llm_onnx.py so the final lm_head matmul is TILED into chunks of vocab
   <= 65535 (e.g. 3 chunks of ~50646, or 4 of ~37984), each a separate MatMul -> logits slice,
   then Concat to 1x1x151936. If the token-embedding Gather materializes a 151936-wide tensor,
   chunk it too. Keep everything else identical to the FULL BF16 config that already passed the
   host quality gate (cosine 0.991).
2. Re-run the FULL BF16 export with the chunked head.
   - If it now PASSES vnn_VerifyGraph and exports an NBG -> the 65536 dimension limit was the
     blocker. Run the host quality gate (must stay cosine > 0.99 vs FP oracle), then go to GATE 3.
   - If it STILL fails -3 with all tensor dims now < 65536 -> the secondary BF16-specific effect
     is real (the unexplained FP16-vs-BF16 asymmetry); record it and go to GATE 2.

GATE 2 (only if chunking alone doesn't fix export — use ACUITY's native hybrid, not hand splits):
1. Use ACUITY's --hybrid flow with --compute-entropy: inspect entropy.txt, promote the highest-
   entropy (outlier) layers AND the lm_head to BF16 via customized_quantize_layers, leave the
   rest INT16, and let ACUITY insert its own DataConvert nodes at the boundaries (do NOT hand-
   place splits on SLICE/MATMUL). Combine with the chunked head from Gate 1.
2. If you must hand-place any BF16<->INT16 boundary, insert an explicit DataConvert/Cast node so
   no SLICE or MATMUL consumes mixed dtypes: BF16 -> DataConvert -> INT16 -> SLICE(INT16->INT16);
   and run an all-INT16 MATMUL then DataConvert the INT16 result to BF16 AFTER it.
3. Optionally try pcq (per-channel INT8) on the linear layers with INT16 activations via hybrid
   (Qwen's outliers are channel-concentrated from large QKV bias terms, so per-channel is the
   right tool), with gentle SmoothQuant alpha <= 0.5 (aggressive smoothing already failed in T7).
   GATE: host export succeeds AND cosine > 0.99 + top-1 match.

GATE 3 (only if a config passes export + host quality): port to the 6 GB Orange Pi Zero 3W
(192.168.31.225) using the T10b runner/VIPLite layout (recompile on board, glibc-matched .so),
smoke-test with the working SmolLM2-135M int16 NBG first, then run the chunked-BF16 Qwen NBG.
Verify coherent text vs oracle; record tok/s, prefill, RSS, NBG size. Then try W=64.

SUCCESS GATE: a Qwen2.5-0.5B that exports to NBG AND runs coherently on the Orange Pi NPU, tok/s
recorded -- OR, if Gate 1 and Gate 2 both fail export, a precise vendor ticket: the chunked
(all-dims-<65536) BF16 graph that still fails vnn_VerifyGraph, attaching the host-coherent proof
and the exact logs, sent to VeriSilicon/Allwinner.

DO NOT retry (confirmed dead ends): full BF16 with UN-chunked 151936 head (verify -3); full FP16
(exports, incoherent 0.541); plain INT16 Qwen (0.236); hand-placed BF16/INT16 splits ON
SLICE/MATMUL nodes; TVM vsi_npu / hand-built TIM-VX (same verifier, no LLM coverage); QDQ-ONNX
import.

START FROM: make_real_llm_onnx.py; the FULL BF16 config that passed host quality in T9; the T7
hybrid/mixed-precision seed tooling; convert_onnx_to_nbg.sh (supports bf16/fp16); host-oracle
tooling; the working SmolLM2-135M int16 NBG; the T10b Orange Pi runner.