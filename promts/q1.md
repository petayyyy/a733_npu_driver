TASK Q1-qwen-int8-gates: Two research passes converged: the BF16 path is a dead end (compiler
wall), but two UN-tried integer paths could unlock Qwen2.5-0.5B on the NPU because they keep all
dtype boundaries integer (int8<->int16), which the verifier already accepts for shipping vision
models. Test BOTH cheaply on host BEFORE any board work. Do NOT touch BF16.

CONTEXT (verified, treat as given):
- Qwen2.5-0.5B (24 layers, hidden 896, 14 q / 2 kv heads, vocab 151936, rope_theta 1e6, QKV bias)
  has large activation outliers (act_absmax ~1790). Builder is verified correct (FP32 ONNX vs HF
  oracle cosine ~1.0). Dead ends: plain int16 (cosine 0.236), full FP16 (0.541), all BF16 configs
  (vnn_VerifyGraph -3 / 64768, or PERMUTE/DataConvert 65280). Earlier W8A16 failed ONLY because
  SmoothQuant alpha=0.5 corrupted even the int16 control (confounded, not disproven).
- ACUITY tensor dim limit 65536, so keep lm_head chunked < 50646 (3 chunks) in all attempts.
- Legal boundaries are integer-to-integer here; verify the exact INT8<->INT16 in/out legality for
  MATRIXMUL/PERMUTE/SLICE/DATACONVERT in TIM-VX source first (same method used for the BF16 matrix).

GATE A -- Per-channel INT16 (cheapest, ~minutes):
1. Convert Qwen2.5-0.5B W=32 with `--quantizer perchannel_symmetric_affine --qtype int16`
   (chunked lm_head < 50646). 
2. Two outcomes: (i) pegasus REJECTS per-channel for target VIP9000NANODI_PLUS_PID0X1000003B
   (it may be chip-ID-gated; the only explicit gating statement names chip 0xBE/"T3" in the
   Amlogic SDK) -> record as a confirmed dead end for this chip, go to Gate B. (ii) it ACCEPTS ->
   run host quality vs FP oracle. GATE: cosine > 0.90 + top-1 match AND NBG export status 0.

GATE B -- Correct W8A16 (per-channel/per-tensor int8 weights + per-tensor int16 activations):
1. Configure: INT8 weights (try per-channel; fall back to per-tensor INT8 if per-channel is
   chip-gated per Gate A), INT16 activations everywhere, RMSNorm + lm_head at highest int
   precision, lm_head chunked < 50646.
2. SmoothQuant alpha SWEEP -- do NOT hardcode 0.5. Run alpha in {none, 0.2, 0.5, 0.7} (apply the
   per-channel activation->weight migration offline in PyTorch before ONNX export). For each
   alpha, compute ACUITY host int8/int16 sim cosine vs FP oracle. Pick the best alpha by cosine.
   (Rationale: int16 activations already hold the outliers, so light/no smoothing may win; but
   very large outliers may need MORE migration to the int16-protected weights -- let the sweep
   decide.)
3. GATE: best-alpha host cosine > 0.90 + top-1 match AND NBG export returns status 0 (no -3, no
   65280). Verify the export has only int8/int16 boundaries (no BF16 anywhere).

IF either gate passes export + quality: port to the Orange Pi Zero 3W (192.168.31.225) via the
T10b runner (recompile on board, glibc-matched /home/orangepi/lib .so), smoke-test the working
SmolLM2-135M int16 NBG first, then run the Qwen NBG. Verify coherent text vs oracle; record decode
tok/s, prefill, RSS, NBG size; try W=64.

DELIVERABLE: reports/q1-qwen-int8-gates.md with, per gate: pegasus accept/reject, best alpha + its
host cosine, export status + error code if failed, and (if exported) board coherence + tok/s/RSS.
Update the confirmed-dead-ends list. Mark each result verified.

SUCCESS GATE: a Qwen2.5-0.5B that exports (int8/int16 only) AND is coherent on the Orange Pi NPU,
tok/s recorded -- OR both gates documented as failing with exact reasons (chip-gating / export
code), which decides the project: hybrid CPU-LLM + NPU-vision becomes the path.

DO NOT retry: any BF16 config; BF16<->INT16 boundaries on SLICE/MATMUL/PERMUTE; forcing
BF16->F16->INT16 via a pegasus flag (no such control); QDQ-ONNX import; W8A16 with a single
hardcoded alpha=0.5.

START FROM: make_real_llm_onnx.py (chunked lm_head); convert_onnx_to_nbg.sh; the host-oracle
tooling; the T7 mixed-precision/seed tooling; the working SmolLM2-135M int16 NBG; T10b runner.