TASK B3-vlm-orangepi: VLM was proven on the Radxa earlier (MobileCLIP-S0 vision encoder on NPU,
cosine 0.9998; tiny VLM bridge image-embed + projector + Gather + decoder all on NPU) but has
NEVER been validated on the final Orange Pi Zero 3W. Close that gap: confirm the vision-encoder-
on-NPU + LLM-decoder pipeline actually runs end-to-end on the Orange Pi.

DO:
1. Port the WORKING MobileCLIP-S0 vision encoder int16 NBG to the Orange Pi (same silicon,
   cid=0x1000003b, so the NBG should transfer as-is). Run it via the board's VIPLite runtime
   (the T10b layout: /home/orangepi/lib, glibc-matched .so). Verify the 1x512 image embedding
   matches the Radxa/ACUITY result (cosine vs host reference). Record encode latency + RSS.
2. Port the tiny VLM bridge NBG (image embedding + projector + token Gather + concat + decoder +
   logits, all in one NBG) to the Orange Pi and run it end-to-end: feed a real preprocessed
   image embedding + a short text token window, get logits, verify coherent next-token vs the
   host reference. Record tok/s + RSS.
3. Document the realistic VLM story on this board: which parts run on the NPU (vision encoder +
   bridge), what a full small-VLM (e.g. SmolVLM / nanoLLaVA-class) would need, and the honest
   limitations (fixed window for the text side, the same int16-outlier risk for a larger LLM
   decoder, image preprocessing on CPU). State clearly whether VLM-on-NPU is a working story on
   the Orange Pi or only a partial/proof-of-concept one.

DELIVERABLE: reports/b3-vlm-orangepi.md with the vision-encoder validation (cosine, latency, RSS),
the VLM-bridge end-to-end result, and an honest assessment of how usable VLM-on-NPU is on this
board. Mark each claim verified (measured on board) or host-only.

SUCCESS GATE: the MobileCLIP-S0 vision encoder runs on the Orange Pi NPU with output matching the
reference, AND the VLM bridge runs end-to-end on the board -- OR a precise blocker if something in
the VLM path fails on this board's VIPLite. Committed.

START FROM: the working MobileCLIP-S0 vision NBG and tiny VLM bridge NBG (reports
g3a-mobileclip-s0-vision.md, g3a-tiny-vlm-bridge-npu.md); the T10b Orange Pi runner and VIPLite
layout; the host references for both.