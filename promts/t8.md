TASK T8-qwen-int16-port: STOP optimizing. The actual goal — Qwen2.5-0.5B int16 on the
6 GB Orange Pi Zero 3W — has NOT been done; all Qwen work so far ran on the 1 GB Radxa
where it OOMs. The 6 GB board removes that RAM wall. Also: plain un-smoothed int16 Qwen
coherence was NEVER verified (it OOM'd on the Radxa before executing, and the host-oracle
tooling didn't exist at T4). Do NOT use SmoothQuant here — it corrupted the int16 control
in T7. W8A16 is a separate later task.

STEP A — host coherence gate for UN-SMOOTHED int16 Qwen (cheap, do first):
1. Take the existing un-smoothed full Qwen2.5-0.5B W=32 int16 package
   (work/model-packages/qwen25_05b_w32_int16, NBG ~1.06 GB). NO SmoothQuant.
2. Run it through the T7 host-oracle gate: dump_real_llm_oracle.py for the FP reference,
   then compare_acuity_host_to_oracle.py on the ACUITY host simulation.
3. GATE A: if logits cosine vs FP oracle > 0.99 and top-1 matches, int16 Qwen is coherent
   -> proceed to Step B. If it fails, that is a major new finding (int16 Qwen itself is
   broken) -> stop and report before any board work.

STEP B — port to the 6 GB Orange Pi Zero 3W and run it (the goal):
1. Flash the Orange Pi Zero 3W official image 1.0.4 from orangepi.org (A733 Zero 3W, NOT
   the H618 Zero 3). Confirm boot, kernel 6.1.31, `free -h` shows ~6 GB.
2. NPU bring-up on this BSP: confirm /dev/vipcore; get the VIPLite runtime + kernel NPU
   module from the Orange Pi BSP; pick the VIPLite .so from ai-sdk that matches the board's
   glibc (e.g. the glibc-gccXX build, like Rabs9's glibc-gcc13_2_0 path — not the Radxa one).
3. Recompile the T1 persistent runner on bookworm (glibc 2.36); do NOT reuse the bullseye
   binary.
4. Smoke-test the port with the WORKING SmolLM2-135M W=32 int16 NBG (unchanged) -> confirm
   it still prints "The capital of France is Paris...". This proves the port before Qwen.
5. Upload the un-smoothed int16 Qwen NBG and run it via the runner. Verify coherent text,
   record decode tok/s, prefill, first-token latency, peak RSS, NBG size. Then try W=64.

SUCCESS GATE: Qwen2.5-0.5B-Instruct produces coherent text with every layer on the Orange
Pi Zero 3W NPU (int16), tok/s + RSS recorded. This is the project goal.

START FROM: work/model-packages/qwen25_05b_w32_int16; T7 host-oracle tooling
(dump_real_llm_oracle.py, compare_acuity_host_to_oracle.py); T1 runner; the working
SmolLM2-135M int16 NBG; orangepi.org Zero 3W 6 GB image; Rabs9 repo as A733-kernel reference.