TASK V2-hybrid-vlm-npu-offload (run AFTER V1 picks a winning VLM): Move the WINNING VLM's vision
encoder onto the NPU (it currently runs on CPU via llama.cpp mmproj) to offload the A76 cores,
while keeping the LLM decoder on CPU. Goal: the same image-chat as V1 but with vision compute on
the NPU. We already proved a vision encoder runs on this NPU (MobileCLIP-S0, 22.6 ms, cosine
0.99996 on the Orange Pi), so the path is real.

DO:
1. Identify the winning VLM's vision encoder (e.g. SigLIP / InternViT) and export it to ONNX with
   fixed input shape, then convert to an int16 NBG via convert_onnx_to_nbg.sh. Validate the NBG
   image-embedding output against the CPU mmproj embedding for the same image (cosine gate on host
   first). Watch for the same int16-outlier risk seen on LLMs -- if int16 host cosine is poor, this
   encoder may not be int16-friendly; record that.
2. Wire the pipeline: image -> CPU preprocess -> NPU vision-encoder NBG -> projector -> the CPU
   llama.cpp decoder (feed the NPU-produced image embedding in place of mmproj's). Keep tokenize/
   KV-cache/sampling/LLM on CPU.
3. Validate end-to-end on the Orange Pi: same test images/questions as V1; confirm the answer is
   still accurate (compare to V1's CPU-only answer). Measure: vision-encoder latency on NPU,
   end-to-end answer latency, decode tok/s, peak RSS, and how much CPU is freed vs V1 (the A76
   cores no longer running the encoder).
4. If the winning VLM's encoder will NOT convert/export to a coherent NBG, fall back to running
   MobileCLIP-S0 (proven on NPU) as the encoder if the decoder can consume its embedding, OR
   document that this VLM's vision-on-NPU is blocked and V1's CPU-only config is the deliverable.

DELIVERABLE: reports/v2-hybrid-vlm-npu-offload.md: encoder NBG export + host cosine, end-to-end
on-board accuracy vs V1, vision latency on NPU, decode tok/s, RSS, and CPU freed. State clearly
whether the hybrid (NPU vision + CPU LLM) works for this VLM or the encoder is NPU-blocked.

SUCCESS GATE: the winning VLM runs end-to-end on the Orange Pi with its vision encoder on the NPU
and the LLM on CPU, answer still accurate, CPU offload quantified -- OR a documented encoder-export
blocker with V1's CPU-only config as the fallback deliverable. Committed.

START FROM: the V1 winning VLM + its vision encoder; convert_onnx_to_nbg.sh; the proven
MobileCLIP-S0 NBG and the B3 vision-on-NPU pipeline; the T10b runner; host-oracle tooling.