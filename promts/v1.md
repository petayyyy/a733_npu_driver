TASK V1-hybrid-vlm-cpu: Build a working image+text VLM chat on the Orange Pi Zero 3W, running
ENTIRELY on CPU first via llama.cpp (this is the proven, low-risk path; vision-on-NPU offload is
the next task). Try several small VLMs, measure each, and STOP on the best-accuracy config that
actually runs (or the only one that runs). This serves the user's goal of a local image assistant
(picoclaw + local VLM) on the 6 GB board.

CANDIDATES (try in this order; all via llama.cpp with the model's mmproj vision projector):
1. SmolVLM-256M-Instruct (smallest/fastest)
2. SmolVLM-500M-Instruct
3. InternVL3.5-1B  (the user liked this one on RK3588; the smartest candidate)
4. nanoLLaVA / LLaVA-1.5-class ~1B if the above don't cover the quality range
Use Q8_0 (and Q4_K_M as a speed alternative) GGUF + the matching mmproj.

DO (on the Orange Pi, 192.168.31.225, full CPU; pin LLM threads to the 2 A76 cores):
1. Build/confirm llama.cpp with multimodal (mmproj) support on this board.
2. For EACH candidate that loads: feed a fixed test image (use the dog_224 image already in the
   repo's resnet50 input_data, plus 1-2 more varied images) and a fixed question ("Describe this
   image." / "What animal is in this image?"). Capture: the generated answer, decode tok/s,
   prefill tok/s, first-token latency, peak RSS, and whether the answer is ACCURATE (correct
   description of the image content -- judge against the known image).
3. Rank by ACCURACY first, then speed. If only one candidate runs, that is the result.
4. Note RAM headroom (free -h) so the user knows what's left for picoclaw/ROS2.

DELIVERABLE: reports/v1-hybrid-vlm-cpu.md with a table (model x quant -> loads y/n, answer
accuracy, decode tok/s, first-token ms, peak RSS), the chosen best-accuracy config, and the
full chat transcript for the winner. Mark numbers verified.

SUCCESS GATE: at least one VLM produces an accurate image description on the Orange Pi CPU, with
the best-accuracy runnable config selected and benchmarked. Committed.

START FROM: the B4 llama.cpp build; HF GGUF + mmproj for SmolVLM-256M/500M and InternVL3.5-1B;
test images in the repo (resnet50 input_data) + a couple more.