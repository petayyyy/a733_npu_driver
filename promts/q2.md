TASK Q2-qwen-block-nbg (run ONLY if Q1 both gates failed to export): The monolithic Qwen NBG hits
a residual vnn_VerifyGraph -3 even with all tensor dims < 65536, suggesting an aggregate-graph
limit (an unexplained "network size 2048" is referenced in the i.MX 8M Plus RM). VIPLite supports
Multi-Graph execution (verified), so test whether compiling Qwen as per-decoder-block NBGs chained
at runtime sidesteps the aggregate limit. Use whichever quantization passed Q1's HOST quality gate
(per-channel int16, or W8A16 with the best alpha) -- NOT BF16.

GATE 1 -- single block (make-or-break, cheap):
1. Export ONE Qwen2.5 decoder block to ONNX (fixed W=32, fixed-size KV-window inputs), quantize
   with the Q1-best integer recipe, compile to NBG.
2. GATE: the single block exports (status 0) AND its host output matches the FP oracle for that
   block (cosine > 0.95). If a single block STILL fails -3 -> the limit is not aggregate; STOP and
   escalate to vendor (this is the genuine blocker). If it passes -> proceed.

GATE 2 -- chain all 24 blocks at runtime:
1. Compile each of the 24 decoder blocks (+ embedding handling + chunked lm_head) as separate NBGs.
   Keep token embedding and the final lm_head as their own stages.
2. In the runner, execute block N, pass its hidden-state output tensor as block N+1's input
   (host-side memcpy between NBG executions via VIPLite Multi-Graph). Implement the fixed-window
   decode loop across the chained blocks.
3. Validate full-model coherence vs the FP oracle on host first, then on the Orange Pi.
4. Measure the per-token dispatch overhead of 24 chained NBGs (this is the main cost vs a
   monolith); report decode tok/s, prefill, RSS.

DELIVERABLE: reports/q2-qwen-block-nbg.md: single-block export+accuracy, the chaining mechanism,
full-model coherence, and tok/s/RSS/overhead on board -- OR the single-block failure that confirms
vendor escalation. Mark each result verified.

SUCCESS GATE: a block-chained Qwen2.5-0.5B coherent on the Orange Pi NPU with tok/s recorded, OR a
documented single-block -3 failure proving the limit is not aggregate (-> vendor).

START FROM: make_real_llm_onnx.py (add per-block ONNX export); the Q1 best integer quantization;
VIPLite Multi-Graph in the T10b runner; host-oracle tooling.