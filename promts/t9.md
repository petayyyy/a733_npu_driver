TASK T9-qwen-bf16: T8 found un-smoothed int16 Qwen2.5-0.5B fails the host gate
(logits cosine 0.236 vs FP oracle). DO NOT conclude "Qwen is broken" — the ONNX builder
(make_real_llm_onnx.py) correctly reads Qwen's config (rope_theta=1e6, QKV bias, GQA,
rms_eps), and the same builder produced a coherent SmolLM2-135M. The likely cause is that
ACUITY "int16" is DYNAMIC FIXED POINT (single power-of-2 scale per tensor), which cannot
represent Qwen2.5's large activation outliers (T7 saw act_absmax ~1790); the log even shows
logits saturating (int16 fl=10 range +-32, max_abs_diff 34). SmolLM2 lacks these outliers,
so its int16 works. Note int16-dfp is NOT the same as IEEE fp16/bf16, which the VIP9000
supports natively and which handle outliers gracefully.

STEP 1 — Diagnose builder vs quantization (host, cheap, do first):
1. Run the builder's real_llm.onnx for Qwen in FP32 via onnxruntime on the W=32 token window.
2. Compare its logits to the FP oracle (dump_real_llm_oracle.py / compare tooling).
3. If cosine > 0.99 -> builder is correct, the int16 quantization is the culprit -> Step 2.
   If cosine is low -> Qwen-specific builder bug: dump per-layer outputs vs oracle to find
   the first layer/path that collapses, fix it, re-test. Report findings.

STEP 2 — BF16 (or FP16) export, the likely fix (host gate first):
1. Extend scripts/host/convert_onnx_to_nbg.sh to accept --quant bf16 (and fp16): pass it as
   the pegasus quantize qtype. Verify the ACUITY CLI accepts bf16 on this build
   (ubuntu-npu:v2.0.10.1). If bf16 is rejected, try fp16; if both are rejected, fall back to
   per-layer int16 widening on the outlier layers (the T7 widening mechanism, applied to int16).
2. Export Qwen2.5-0.5B W=32 as a BF16 NBG (~1 GB, fits the 6 GB board).
3. Host gate: compare BF16 ACUITY host logits to the FP oracle. GATE: cosine > 0.99 and
   top-1 match.

STEP 3 — Port to the 6 GB Orange Pi Zero 3W and run it (only if Step 2 passes the gate):
1. Flash Orange Pi Zero 3W official image 1.0.4 (A733, NOT H618); confirm kernel 6.1.31,
   ~6 GB. Bring up /dev/vipcore; take VIPLite + NPU kernel module from the OPi BSP; pick the
   ai-sdk VIPLite .so matching the board glibc (glibc-gccXX build, like Rabs9's). Recompile
   the T1 runner on bookworm.
2. Smoke-test with the working SmolLM2-135M int16 NBG (must still print "...Paris...").
3. Run the BF16 Qwen NBG via the runner. Verify coherent text vs FP oracle; record tok/s,
   prefill, latency, peak RSS, NBG size. Then try W=64.

SUCCESS GATE: Qwen2.5-0.5B coherent with every layer on the Orange Pi Zero 3W NPU (BF16),
tok/s + RSS recorded. If BF16/FP16 are both unsupported AND per-layer int16 widening can't
recover coherence, that is the real, logged finding (then int16 is genuinely insufficient
for Qwen on this NPU and SmolLM2 stays the production model).

START FROM: work/generated/qwen25_05b_w32/real_llm.onnx; T7/T8 host-oracle tooling;
convert_onnx_to_nbg.sh; the working SmolLM2-135M int16 NBG; orangepi.org Zero 3W 6 GB image.