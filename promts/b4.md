TASK B4-qwen-cpu-baseline: Measure what Qwen2.5-0.5B-Instruct can do on the Orange Pi Zero 3W
CPU (NOT the NPU) at maximum context and best settings, using llama.cpp with a real KV-cache.
This is the honest fallback baseline for when ROS2 is paused/frozen and the full CPU is available
-- it answers "what can I actually get from Qwen on my own hardware in that mode". This is a CPU
benchmark on purpose; Qwen-on-NPU is vendor-blocked and out of scope here.

DO:
1. Build llama.cpp for aarch64 on the Orange Pi (bookworm/this image), with NEON/dotprod
   optimizations. Pin threads to the 2 Cortex-A76 cores (decode is memory-bound; in earlier
   measurements 2 A76 threads beat 4/8 -- confirm the optimal -t on this board).
2. Get Qwen2.5-0.5B-Instruct in GGUF (Q4_K_M, and also Q8_0 for a quality/speed comparison).
3. Benchmark with llama-bench AND a real chat run (llama-cli with the Qwen chat template):
   - Context sizes: try the MAXIMUM practical context the 5.7 GB board allows -- Qwen2.5-0.5B
     supports up to 32k; sweep e.g. 2k, 8k, 16k, 32k. Report where RAM or speed makes it
     impractical.
   - Record: prompt/prefill tok/s, generation tok/s (at short AND long context, since KV-cache
     means decode stays roughly constant -- verify this), first-token latency, peak RSS per
     context size, for Q4_K_M and Q8_0.
4. Also do ONE real long-context chat demo (e.g. paste a long passage + ask a question) to show
   what the full-CPU, long-context experience actually feels like, with timing.
5. Briefly contrast with the NPU path: full real context (thousands of tokens, KV-cache) and
   coherent Qwen quality -- the two things the NPU fixed-window int16 path cannot give -- vs the
   NPU's value (frees the A76 cores). Frame it as "ROS2-frozen -> use this CPU path; ROS2-running
   -> NPU SmolLM2 keeps the A76 cores free".

DELIVERABLE: reports/b4-qwen-cpu-baseline.md with a table (quant x context -> prefill tok/s,
decode tok/s, first-token ms, peak RSS), the long-context chat demo transcript with timing, and
the optimal thread/quant/context recommendation for the ROS2-frozen mode. Mark each number
verified (measured) or estimate.

SUCCESS GATE: measured Qwen2.5-0.5B CPU numbers across quant and context sizes on the Orange Pi,
including the maximum practical context, with a clear recommendation. Committed.

START FROM: the existing llama.cpp CPU baseline work (reports g3a-llama-cpp-decoder.md, the
measured SmolLM2 56.7 tok/s 2-thread result); the Orange Pi board (192.168.31.225); Qwen2.5-0.5B
GGUF from HF.