TASK B1-benchmark-matrix: Produce a complete, honest benchmark matrix of every available
SmolLM2 weight at every window size, NPU-only on the Orange Pi Zero 3W (192.168.31.225).
The goal is a single table: for each (model, W) -> coherent? tok/s, prefill, RSS, NBG size.

MODELS: SmolLM2-135M-Instruct (already works), SmolLM2-360M-Instruct, SmolLM2-1.7B-Instruct.
If other small int16-friendly instruct models are trivially available in the same flow
(e.g. SmolLM2 base variants, Qwen2.5-0.5B is EXCLUDED -- it is vendor-blocked, do not retry),
note them but do not block on them.
WINDOWS: W = 32, 64, 128, 256, for each model.

METHOD (do this per model, cheapest-gate-first so you never waste board time on a broken config):
1. Build the fixed-window ONNX with make_real_llm_onnx.py for each W.
2. HOST GATE (cheap, do before any board work): for each (model, W), run the FP32 ONNX vs the
   FP oracle (compare_onnxruntime_to_oracle.py) AND the int16 ACUITY host sim vs the FP oracle
   (compare_acuity_host_to_oracle.py). Record cosine + top-1 for both.
   - If int16 host cosine < 0.99 -> mark that (model, W) as "int16 incoherent (outliers)" and
     do NOT run it on the board. This is the expected failure mode for the bigger models; the
     research already showed int16 dynamic-fixed-point breaks on activation outliers. Record it
     as a real datapoint, not a bug to fix here.
3. For every (model, W) that PASSES the int16 host gate: convert to int16 NBG, deploy to the
   Orange Pi, run via the persistent runner (the T10b runner, already built for that board's
   VIPLite layout), and measure: decode tok/s, prefill tok/s, first-token latency, peak RSS,
   NBG size. Use the SAME prompt for every run for comparability.
4. Watch RAM: SmolLM2-1.7B int16 NBG is ~1.6 GB; the board has 5.7 GB, so it should fit, but
   confirm with free -h and report actual RSS. Larger W also grows the activation/NBG footprint.

DELIVERABLE: reports/b1-benchmark-matrix.md with ONE master table (rows = model x W; columns =
int16 host cosine, coherent y/n, decode tok/s, prefill tok/s, first-token ms, peak RSS, NBG MB),
plus a short note per model on where int16 stops being coherent and where tok/s becomes
impractical (<~3 tok/s). Mark each number verified (measured on board) or host-only.

SUCCESS GATE: a complete matrix where every cell is either a measured board number or an
explicit "int16 host-incoherent / not run" with its cosine. Committed.

START FROM: make_real_llm_onnx.py; the T7/T8/T9 host-oracle tooling; the T10b Orange Pi runner
and board VIPLite layout (/home/orangepi/lib, glibc-matched .so); HF checkpoints for
SmolLM2-135M/360M/1.7B-Instruct.