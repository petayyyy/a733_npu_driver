TASK T10-qwen-mixed-bf16: T9 proved (a) the Qwen ONNX builder is correct (FP32 vs FP oracle
cosine 0.99999999997), and (b) BF16 fixes Qwen's outlier problem at the QUALITY level (host
cosine 0.991, top-1 match) where int16 fixed-point fails (0.236). BUT full-graph BF16 NBG
export is BLOCKED by ACUITY vnn_VerifyGraph status -3 ("configuration that cannot be
supported"), and FP16 exports but fails quality (cosine 0.54, FP16's 5-bit exponent can't
cover Qwen's ~1790 activation outliers; BF16's 8-bit exponent can). The likely VerifyGraph
trigger is the 151936-wide BF16 logits projection (the big vocab has been the recurring
stress point). Goal: a MIXED-precision package that keeps BF16 only where outliers need it
and avoids the BF16 layout that breaks export.

DO (host-only first, reuse the T7 mixed-precision seed machinery):
1. Build a mixed-precision seed for Qwen W=32: keep the embedding and the lm_head/logits
   projection in int16 (these are the export-breaking big-vocab tensors), and put the
   outlier-heavy transformer activations (especially the gated/MLP projections that T7 showed
   act_absmax ~1790) in BF16. Iterate on WHICH regions need BF16 using the host per-layer
   cosine, not guesswork.
2. GATE A (host): export must succeed (no VerifyGraph -3) AND logits cosine vs FP oracle
   > 0.99 with top-1 match. If export still fails, bisect which BF16 region triggers it
   (binary-search the layers); if quality fails, widen more transformer regions to BF16.
3. If no mixed split satisfies BOTH (export OK + cosine >0.99), STOP and file the precise
   vendor blocker: full + mixed BF16 configs that pass host quality but fail VerifyGraph -3,
   with the exact tensor shapes and the preserved stdout/stderr.

THEN (only if Gate A passes) port to the 6 GB Orange Pi Zero 3W and run it: flash image 1.0.4,
bring up /dev/vipcore, take VIPLite + NPU module from the OPi BSP, pick the glibc-matched
ai-sdk .so, recompile the T1 runner on bookworm, smoke-test with the working SmolLM2-135M
int16 NBG first, then run the mixed-BF16 Qwen NBG; record coherence vs oracle, tok/s, RSS.

SEPARATELY / IN PARALLEL (TASK T10b, independent, low-risk — do this regardless):
Port the ALREADY-WORKING SmolLM2-135M int16 NBG to the 6 GB Orange Pi Zero 3W and validate
it runs coherently there (the OPi port itself has never been tested). This completes the
core "LLM on NPU" goal on the final board independent of the Qwen quantization fight.

SUCCESS GATE: either a mixed-BF16 Qwen that exports AND is coherent on the OPi NPU, OR a
filed precise vendor blocker for the BF16 VerifyGraph -3 — plus, independently, SmolLM2-135M
validated coherent on the Orange Pi NPU.

DO NOT retry: full-graph BF16 export (blocked, VerifyGraph -3); full FP16 (exports but
incoherent, cosine 0.54); plain int16 Qwen (incoherent, 0.236); QDQ-ONNX import; the broken
ACUITY hybrid YAML path on the full vocab.

START FROM: work/generated/qwen25_05b_w32/real_llm.onnx; the working BF16/int16/fp16 packages
and host-oracle tooling from T7/T8/T9; convert_onnx_to_nbg.sh (now supports bf16/fp16);
the working SmolLM2-135M int16 NBG; orangepi.org Zero 3W 6 GB image.