# Historical Report - llama.cpp CPU Decoder Baseline

Date: 2026-06-20 local / 2026-06-20 UTC

## Superseded Status

This report is retained as a historical CPU diagnostic baseline only. It does
not satisfy the active project requirement. The current user requirement is that
LLM/VLM model-layer compute must run on the A733 NPU, not on the CPU.

## Purpose

This historical report recorded an earlier CPU-side decoder experiment. The NPU
vision-encoder subgate is already proven with MobileCLIP-S0; this step only
checks that a small GGUF language model can be built and run through llama.cpp
on the Radxa Cubie A7Z CPU cores.

This is not an accepted end-to-end image-to-text pipeline. It records a CPU
runtime baseline for diagnostics only.

## Board Environment

- Board: Radxa Cubie A7Z
- OS: Debian GNU/Linux 11 (bullseye)
- Kernel: `5.15.147-21-a733`
- CPU cores: 8
- Memory at build start: `959MiB` RAM, `2.5GiB` swap
- Disk: `/dev/mmcblk0p3`, about `24GiB` free during the run

## llama.cpp Build

- Repository: `https://github.com/ggml-org/llama.cpp.git`
- Commit: `f449e0553708b895adbd94a301431cef691f632d`
- ggml version: `0.15.2`
- Compiler: GCC/G++ `10.2.1`
- CMake: `3.18.4`
- CMake flags:
  - `-DCMAKE_BUILD_TYPE=Release`
  - `-DGGML_NATIVE=ON`
  - `-DGGML_OPENMP=ON`
  - `-DLLAMA_BUILD_TESTS=OFF`

The build detected the A733 CPU as ARM and selected:

```text
-mcpu=cortex-a76.cortex-a55+crypto+dotprod+noi8mm+nosve
```

The current upstream unified `llama-app` target did not link on this board
because it expected `llama-server-impl` and `llama-cli-impl` libraries that were
not generated by the target graph. This did not block the decoder subgate:
separate executable targets built successfully:

```text
llama-simple
llama-simple-chat
llama-bench
```

The reusable board script therefore builds those separate targets by default:

```bash
scripts/board/build-llama-cpp.sh
```

## Test Model

- GGUF repo: `bartowski/SmolLM2-135M-Instruct-GGUF`
- Base model: `HuggingFaceTB/SmolLM2-135M-Instruct`
- File: `SmolLM2-135M-Instruct-Q4_K_M.gguf`
- License: Apache-2.0
- Downloaded size on board: `101M`
- llama-bench model size: `98.87 MiB`
- Parameters: `134.52M`
- SHA-256:

```text
2e8040ceae7815abe0dcb3540b9995eaa1fa0d2ca9e797d0a635ae4433c68c2d
```

## Benchmark

Command:

```bash
llama-bench \
  -m /home/radxa/a733_npu_driver/models/gguf/SmolLM2-135M-Instruct-Q4_K_M.gguf \
  -p 128 -n 64 -t 1,2,4,8 -ngl 0 -r 3 -o md
```

Result:

| Threads | Prompt eval `pp128` | Decode `tg64` |
|---:|---:|---:|
| 1 | 11.98 tok/s | 9.22 tok/s |
| 2 | 96.24 tok/s | 56.74 tok/s |
| 4 | 108.63 tok/s | 46.00 tok/s |
| 8 | 122.57 tok/s | 46.86 tok/s |

For this tiny model, 2 threads produced the best decode throughput while 8
threads produced the best prompt throughput. Larger models should be benchmarked
separately because memory bandwidth and cache pressure will dominate more
strongly.

## Generation Smoke Test

The smoke test used an explicit SmolLM chat prompt:

```text
<|im_start|>system
You are a concise embedded AI assistant.<|im_end|>
<|im_start|>user
Write one short sentence about embedded AI.<|im_end|>
<|im_start|>assistant
```

Generated answer:

```text
Embedded AI is a type of artificial intelligence that uses machine learning
algorithms to learn and adapt in real-time, allowing it to learn from its
environment and respond accordingly.
```

Timings from `llama-simple`:

| Metric | Value |
|---|---:|
| Load time | `1360.90 ms` |
| Prompt eval | `639.19 ms / 30 tokens`, `46.93 tok/s` |
| Decode eval | `1136.33 ms / 34 runs`, `29.92 tok/s` |
| Total | `2515.07 ms / 64 tokens` |

The `llama-simple-chat` binary also accepted stdin and produced a response in
non-interactive mode, which is enough for follow-on automation.

## Raw Logs

The raw board archive is stored locally under ignored logs:

```text
logs/board/g3a-llama-decode.tar.gz
logs/board/g3a-llama-decode-llama-bench-smollm2-q4km.md
logs/board/g3a-llama-decode-llama-simple-chatprompt-smollm2-q4km.txt
logs/board/g3a-llama-decode-llama-simple-chat-smollm2-q4km.txt
```

## Result

As a historical CPU diagnostic baseline, llama.cpp builds on the Radxa Cubie
A7Z, a Q4_K_M SmolLM2 model loads, prompt and decode timings are measurable,
and non-interactive text generation works.

This result is not an accepted G3a subgate under the active NPU-only
requirement. The accepted path is the NPU language graph documented in
`g3a-tiny-lm-gather-npu.md`, followed by an NPU-side VLM projector/decoder path.
